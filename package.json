{
  "name": "pop-chat-backend",
  "version": "1.0.0",
  "description": "Test commit pr",
  "main": "index.js",
  "scripts": {
    "start": "npm run build && npm run serve",
    "serve": "node -r dotenv/config build/server.js",
    "build": "npm run clean && npm run build-ts",
    "build-ts": "tsc",
    "clean": "rimraf ./build",
    "watch-node": "nodemon -r dotenv/config build/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.21",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3",
    "nodemon": "^3.0.2",
    "dotenv": "^16.3.1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "axios": "^1.6.5",
    "bcrypt": "^5.1.1"
  }
}
